import React from 'react';
import ReactDOM from 'react-dom/client';
import ShoppingCartMFE from './ShoppingCartMFE';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ShoppingCartMFE />
  </React.StrictMode>
);
